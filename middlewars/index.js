const validate = require("./validateBody");
const isValidId = require("./isValidId");
const validFavorite = require("./validateBody");

module.exports = {
  validate,
  isValidId,
  validFavorite,
};
